'use strict';

/**
 * @ngdoc directive
 * @name izzyposWebApp.directive:adminPosHeader
 * @description
 * # adminPosHeader
 */

angular.module('sbAdminApp')
  .directive('sidebarSearch', ['CONFIG', function(CONFIG) {
    return {
      templateUrl:'scripts/directives/sidebar/sidebar-search/sidebar-search.html?v='+CONFIG.version,
      restrict: 'E',
      replace: true,
      scope: {
      },
      controller:function($scope){
        $scope.selectedMenu = 'home';
      }
    }
  }]);
